# xorm v1 synchronization

This branch carries the GitHub Actions workflow that mirrors only the `v1`
branch from `https://gitea.com/xorm/xorm.git` to this repository.

The workflow runs daily at 01:17 UTC (09:17 Asia/Shanghai) and can also be
started manually. It does not synchronize tags or any other branch.
